<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>转生成蜘蛛子</title>
  
  <subtitle>记录日常笔记又怎样</subtitle>
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2025-01-02T16:47:34.603Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>蜘蛛子</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Go语言基础之变量和常量</title>
    <link href="http://example.com/2025/01/02/Learn-go-01/"/>
    <id>http://example.com/2025/01/02/Learn-go-01/</id>
    <published>2025-01-01T16:00:00.000Z</published>
    <updated>2025-01-02T16:47:34.603Z</updated>
    
    <content type="html"><![CDATA[<p>题外话：git部署本地<code>hexo s</code> 生成<code>hexo g</code> 部署<code>hexo d</code></p><p>来源是b站<a href="https://www.bilibili.com/video/BV1Fb411M7sg?spm_id_from=333.788.videopod.sections&amp;vd_source=d327742890f6b7694d0a202635037cec">Go语言基础02 变量与常量</a></p><p>变量和常量是编程中必不可少的部分，也是很好理解的一部分。</p><h2 id="标识符与关键字"><a class="markdownIt-Anchor" href="#标识符与关键字"></a> 标识符与关键字</h2><h3 id="标识符"><a class="markdownIt-Anchor" href="#标识符"></a> 标识符</h3><p>在编程语言中标识符就是程序员定义的具有特殊意义的词，比如变量名、常量名、函数名等等。 Go语言中标识符由字母数字和<code>_</code>(下划线）组成，并且只能以字母和<code>_</code>开头。 举几个例子：<code>abc</code>, <code>_</code>, <code>_123</code>, <code>a123</code>。</p><h3 id="关键字"><a class="markdownIt-Anchor" href="#关键字"></a> 关键字</h3><p>关键字是指编程语言中预先定义好的具有特殊含义的标识符。 关键字和保留字都不建议用作变量名。</p><p>Go语言中有25个关键字：</p><pre><code>break        default      func         interface    selectcase         defer        go           map          structchan         else         goto         package      switchconst        fallthrough  if           range        typecontinue     for          import       return       var</code></pre><p>此外，Go语言中还有37个保留字。</p><pre><code>Constants:    true  false  iota  nil    Types:    int  int8  int16  int32  int64                uint  uint8  uint16  uint32  uint64  uintptr              float32  float64  complex128  complex64              bool  byte  rune  string  errorFunctions:   make  len  cap  new  append  copy  close  delete             complex  real  imag             panic  recover</code></pre><h2 id="变量"><a class="markdownIt-Anchor" href="#变量"></a> 变量</h2><h3 id="变量的来历"><a class="markdownIt-Anchor" href="#变量的来历"></a> 变量的来历</h3><p>程序运行过程中的数据都是保存在内存中，我们想要在代码中操作某个数据时就需要去内存上找到这个变量，但是如果我们直接在代码中通过内存地址去操作变量的话，代码的可读性会非常差而且还容易出错，所以我们就利用变量将这个数据的内存地址保存起来，以后直接通过这个变量就能找到内存上对应的数据了。</p><h3 id="变量类型"><a class="markdownIt-Anchor" href="#变量类型"></a> 变量类型</h3><p>变量（Variable）的功能是存储数据。不同的变量保存的数据类型可能会不一样。经过半个多世纪的发展，编程语言已经基本形成了一套固定的类型，常见变量的数据类型有：整型、浮点型、布尔型等。</p><p>Go语言中的每一个变量都有自己的类型，并且变量必须经过声明才能开始使用。</p><h3 id="变量声明"><a class="markdownIt-Anchor" href="#变量声明"></a> 变量声明</h3><p>Go语言中的变量需要声明后才能使用，同一作用域内不支持重复声明。 并且Go语言的变量声明后必须使用。</p><h4 id="标准声明"><a class="markdownIt-Anchor" href="#标准声明"></a> 标准声明</h4><p>Go语言的变量声明格式为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">var 变量名 变量类型</span><br></pre></td></tr></table></figure><p>变量声明以关键字<code>var</code>开头，变量类型放在变量的后面，行尾无需分号。 举个例子：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">var name string</span><br><span class="line">var age int</span><br><span class="line">var isOk bool</span><br></pre></td></tr></table></figure><h4 id="批量声明"><a class="markdownIt-Anchor" href="#批量声明"></a> 批量声明</h4><p>每声明一个变量就需要写<code>var</code>关键字会比较繁琐，go语言中还支持批量变量声明：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">var (</span><br><span class="line">    a string</span><br><span class="line">    b int</span><br><span class="line">    c bool</span><br><span class="line">    d float32</span><br><span class="line">)</span><br></pre></td></tr></table></figure><h4 id="变量的初始化"><a class="markdownIt-Anchor" href="#变量的初始化"></a> 变量的初始化</h4><p>Go语言在声明变量的时候，会自动对变量对应的内存区域进行初始化操作。每个变量会被初始化成其类型的默认值，例如： 整型和浮点型变量的默认值为<code>0</code>。 字符串变量的默认值为<code>空字符串</code>。 布尔型变量默认为<code>false</code>。 切片、函数、指针变量的默认为<code>nil</code>。<br />当然我们也可在声明变量的时候为其指定初始值。变量初始化的标准格式如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">var 变量名 类型 = 表达式</span><br></pre></td></tr></table></figure><p>举个例子：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">var name string = &quot;Kumoko111&quot;</span><br><span class="line">var age int = 18</span><br></pre></td></tr></table></figure><p>或者一次初始化多个变量</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">var name, age = &quot;Kumoko111&quot;, 20</span><br></pre></td></tr></table></figure><ul><li>类型推导<br />有时候我们会将变量的类型省略，这个时候编译器会根据等号右边的值来推导变量的类型完成初始化。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">var name = &quot;Kumoko111&quot;</span><br><span class="line">var age = 18</span><br></pre></td></tr></table></figure><ul><li>短变量声明<br />在函数内部，可以使用更简略的 <code>:=</code> 方式声明并初始化变量。(一开始学的时候漏了这一句导致后面<code>:=</code>全靠猜这下记忆深刻了)</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line"></span><br><span class="line">import (</span><br><span class="line">&quot;fmt&quot;</span><br><span class="line">)</span><br><span class="line">// 全局变量m</span><br><span class="line">var m = 100</span><br><span class="line"></span><br><span class="line">func main() &#123;</span><br><span class="line">n := 10</span><br><span class="line">m := 200 // 此处声明局部变量m</span><br><span class="line">fmt.Println(m, n)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>匿名变量<br />在使用多重赋值时，如果想要忽略某个值，可以使用<code>匿名变量（anonymous variable）</code>。 匿名变量用一个下划线<code>_</code>表示，例如：</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">func foo() (int, string) &#123;</span><br><span class="line">return 10, &quot;Q1mi&quot;</span><br><span class="line">&#125;</span><br><span class="line">func main() &#123;</span><br><span class="line">x, _ := foo()</span><br><span class="line">_, y := foo()</span><br><span class="line">fmt.Println(&quot;x=&quot;, x)</span><br><span class="line">fmt.Println(&quot;y=&quot;, y)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>匿名变量不占用命名空间，不会分配内存，所以匿名变量之间不存在重复声明。 (在<code>Lua</code>等编程语言里，匿名变量也被叫做哑元变量。)</p><p>注意事项：</p><ul><li>函数外的每个语句都必须以关键字开始（var、const、func等）</li><li><code>:=</code>不能使用在函数外。</li><li><code>_</code>多用于占位，表示忽略值。</li></ul><h2 id="常量"><a class="markdownIt-Anchor" href="#常量"></a> 常量</h2><p>相对于变量，常量是恒定不变的值，多用于定义程序运行期间不会改变的那些值。 常量的声明和变量声明非常类似，只是把<code>var</code>换成了<code>const</code>，常量在定义的时候必须赋值。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">const pi = 3.1415</span><br><span class="line">const e = 2.7182</span><br></pre></td></tr></table></figure><p>声明了<code>pi</code>和<code>e</code>这两个常量之后，在整个程序运行期间它们的值都不能再发生变化了。<br />多个常量也可以一起声明：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">const (</span><br><span class="line">    pi = 3.1415</span><br><span class="line">    e = 2.7182</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>const同时声明多个常量时，如果省略了值则表示和上面一行的值相同。 例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">const (</span><br><span class="line">    n1 = 100</span><br><span class="line">    n2</span><br><span class="line">    n3</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>上面示例中，常量n1、n2、n3的值都是100。</p><h3 id="iota"><a class="markdownIt-Anchor" href="#iota"></a> iota</h3><p><code>iota</code>是go语言的常量计数器，只能在常量的表达式中使用。<br /><code>iota</code>在const关键字出现时将被重置为0。const中每新增一行常量声明将使<code>iota</code>计数一次(iota可理解为const语句块中的行索引)。 使用iota能简化定义，在定义枚举时很有用。</p><p>举个例子：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">const (</span><br><span class="line">n1 = iota //0</span><br><span class="line">n2        //1</span><br><span class="line">n3        //2</span><br><span class="line">n4        //3</span><br><span class="line">)</span><br></pre></td></tr></table></figure><h4 id="几个常见的iota示例"><a class="markdownIt-Anchor" href="#几个常见的iota示例"></a> 几个常见的iota示例</h4><p>使用<code>_</code>跳过某些值</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">const (</span><br><span class="line">n1 = iota //0</span><br><span class="line">n2        //1</span><br><span class="line">_</span><br><span class="line">n4        //3</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p><code>iota</code>声明中间插队</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">const (</span><br><span class="line">n1 = iota //0</span><br><span class="line">n2 = 100  //100</span><br><span class="line">n3 = iota //2</span><br><span class="line">n4        //3</span><br><span class="line">)</span><br><span class="line">const n5 = iota //0</span><br></pre></td></tr></table></figure><p>定义数量级 （这里的<code>&lt;&lt;</code>表示左移操作，<code>1&lt;&lt;10</code>表示将1的二进制表示向左移10位，也就是由<code>1</code>变成了<code>10000000000</code>，也就是十进制的1024。同理<code>2&lt;&lt;2</code>表示将2的二进制表示向左移2位，也就是由<code>10</code>变成了<code>1000</code>，也就是十进制的8。）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">const (</span><br><span class="line">_  = iota</span><br><span class="line">KB = 1 &lt;&lt; (10 * iota)</span><br><span class="line">MB = 1 &lt;&lt; (10 * iota)</span><br><span class="line">GB = 1 &lt;&lt; (10 * iota)</span><br><span class="line">TB = 1 &lt;&lt; (10 * iota)</span><br><span class="line">PB = 1 &lt;&lt; (10 * iota)</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>多个<code>iota</code>定义在一行</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">const (</span><br><span class="line">a, b = iota + 1, iota + 2 //1,2</span><br><span class="line">c, d                      //2,3</span><br><span class="line">e, f                      //3,4</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>补充：这个是位运算，<code>KB = 1 &lt;&lt; (10 * iota)</code>的意思就是将1左移10位。如果1是16位整型，二进制为<code>00000000 00000001</code>，左移10位后变成<code>00000100 00000000</code>。左移的话，如果学过计算机组成原理应该知道有几种左移的方法，这里的左移策略应该是左边超出部分会被扔掉，右边不足的部分用0来填充。所以就16位整型1左移10位的结果来说，就是<code>2^10</code>。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;题外话：git部署本地&lt;code&gt;hexo s&lt;/code&gt; 生成&lt;code&gt;hexo g&lt;/code&gt; 部署&lt;code&gt;hexo d&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;来源是b站&lt;a href=&quot;https://www.bilibili.com/video/BV1Fb411M7</summary>
      
    
    
    
    
    <category term="Golang" scheme="http://example.com/tags/Golang/"/>
    
  </entry>
  
  <entry>
    <title>Go语言基础之基本数据类型</title>
    <link href="http://example.com/2025/01/02/Learn-go-02/"/>
    <id>http://example.com/2025/01/02/Learn-go-02/</id>
    <published>2025-01-01T16:00:00.000Z</published>
    <updated>2025-01-02T16:44:51.297Z</updated>
    
    <content type="html"><![CDATA[<p>来源是b站<a href="https://www.bilibili.com/video/BV1qb411M785?spm_id_from=333.788.videopod.sections&amp;vd_source=d327742890f6b7694d0a202635037cec">Go语言基础02 基本数据类型</a></p><p>Go语言中有丰富的数据类型，除了基本的整型、浮点型、布尔型、字符串外，还有数组、切片、结构体、函数、map、通道（channel）等。Go 语言的基本类型和其他语言大同小异。</p><h2 id="基本数据类型"><a class="markdownIt-Anchor" href="#基本数据类型"></a> 基本数据类型</h2><h3 id="整型"><a class="markdownIt-Anchor" href="#整型"></a> 整型</h3><p>整型分为以下两个大类： 按长度分为：int8、int16、int32、int64 对应的无符号整型：uint8、uint16、uint32、uint64</p><p>其中，<code>uint8</code>就是我们熟知的<code>byte</code>型，<code>int16</code>对应C语言中的<code>short</code>型，<code>int64</code>对应C语言中的<code>long</code>型。</p><table>    <tr>        <th>类型</th>        <th>描述</th>    </tr>    <tr>        <th>uint8</th>        <th>无符号 8位整型 (0 到 255)</th>    </tr>    <tr>        <th>uint16</th>        <th>无符号 16位整型 (0 到 65535)</th>    </tr>    <tr>        <th>uint32</th>        <th>无符号 32位整型 (0 到 4294967295)</th>    </tr>    <tr>        <th>uint64</th>        <th>无符号 64位整型 (0 到 18446744073709551615)</th>    </tr>    <tr>        <th>int8</th>        <th>有符号 8位整型 (-128 到 127)</th>    </tr>    <tr>        <th>int16</th>        <th>有符号 16位整型 (-32768 到 32767)</th>    </tr>    <tr>        <th>int32</th>        <th>有符号 32位整型 (-2147483648 到 2147483647)</th>    </tr>    <tr>        <th>int64</th>        <th>有符号 64位整型 (-9223372036854775808 到 9223372036854775807)</th>    </tr></table><h4 id="特殊整型"><a class="markdownIt-Anchor" href="#特殊整型"></a> 特殊整型</h4><table>    <tr>        <th>类型</th>        <th>描述</th>    </tr>    <tr>        <th>uint</th>        <th>32位操作系统上就是uint32，64位操作系统上就是uint64</th>    </tr>    <tr>        <th>int</th>        <th>32位操作系统上就是int32，64位操作系统上就是int64</th>    </tr>    <tr>        <th>uintptr</th>        <th>无符号整型，用于存放一个指针</th>    </tr></table><ul><li><p>注意：在使用<code>int</code>和<code>uint</code>类型时，不能假定它是32位或64位的整型，而是考虑<code>int</code>和<code>uint</code>可能在不同平台上的差异。</p></li><li><p>获取对象的长度的内建<code>len()</code>函数返回的长度可以根据不同平台的字节长度进行变化。实际使用中，切片或map的元素数量等都可以用<code>int</code>来表示。在涉及到二进制传输、读写文件的结构描述时，为了保持文件的结构不会受到不同编译目标平台字节长度的影响，不要使用<code>int</code>和<code>uint</code>。</p></li></ul><h4 id="数字字面量语法number-literals-syntax"><a class="markdownIt-Anchor" href="#数字字面量语法number-literals-syntax"></a> 数字字面量语法（Number literals syntax）</h4><p>Go1.13版本之后引入了数字字面量语法，这样便于开发者以二进制、八进制或十六进制浮点数的格式定义数字，例如：</p><p><code>v := 0b00101101</code>，代表二进制的 101101，相当于十进制的 45。<code>v := 0o377</code>，代表八进制的 377，相当于十进制的 255。<code>v := 0x1p-2</code>，代表十六进制的 1 除以2²，也就是0.25。</p><p>而且还允许我们用<code>_</code>来分隔数字，比如说：<code>v := 123_456</code>表示v的值等于 123456。</p><p>我们可以借助fmt函数来将一个整数以不同进制形式展示。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line"> </span><br><span class="line">import &quot;fmt&quot;</span><br><span class="line"> </span><br><span class="line">func main()&#123;</span><br><span class="line">// 十进制</span><br><span class="line">var a int = 10</span><br><span class="line">fmt.Printf(&quot;%d \n&quot;, a)  // 10</span><br><span class="line">fmt.Printf(&quot;%b \n&quot;, a)  // 1010  占位符%b表示二进制</span><br><span class="line"> </span><br><span class="line">// 八进制  以0开头</span><br><span class="line">var b int = 077</span><br><span class="line">fmt.Printf(&quot;%o \n&quot;, b)  // 77</span><br><span class="line"> </span><br><span class="line">// 十六进制  以0x开头</span><br><span class="line">var c int = 0xff</span><br><span class="line">fmt.Printf(&quot;%x \n&quot;, c)  // ff</span><br><span class="line">fmt.Printf(&quot;%X \n&quot;, c)  // FF</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="浮点型"><a class="markdownIt-Anchor" href="#浮点型"></a> 浮点型</h3><p>Go语言支持两种浮点型数：<code>float32</code>和<code>float64</code>。这两种浮点型数据格式遵循<code>IEEE 754</code>标准：<code>float32</code>的浮点数的最大范围约为<code>3.4e38</code>，可以使用常量定义：<code>math.MaxFloat32</code>。 <code>float64</code>的浮点数的最大范围约为<code>1.8e308</code>，可以使用一个常量定义：<code>math.MaxFloat64</code>。</p><p>打印浮点数时，可以使用<code>fmt</code>包配合动词<code>%f</code>，代码如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line">import (</span><br><span class="line">        &quot;fmt&quot;</span><br><span class="line">        &quot;math&quot;</span><br><span class="line">)</span><br><span class="line">func main() &#123;</span><br><span class="line">        fmt.Printf(&quot;%f\n&quot;, math.Pi)</span><br><span class="line">        fmt.Printf(&quot;%.2f\n&quot;, math.Pi)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="复数"><a class="markdownIt-Anchor" href="#复数"></a> 复数</h3><p>complex64和complex128</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">var c1 complex64</span><br><span class="line">c1 = 1 + 2i</span><br><span class="line">var c2 complex128</span><br><span class="line">c2 = 2 + 3i</span><br><span class="line">fmt.Println(c1)</span><br><span class="line">fmt.Println(c2)</span><br></pre></td></tr></table></figure><p>复数有实部和虚部，complex64的实部和虚部为32位，complex128的实部和虚部为64位。</p><h3 id="布尔值"><a class="markdownIt-Anchor" href="#布尔值"></a> 布尔值</h3><p>Go语言中以<code>bool</code>类型进行声明布尔型数据，布尔型数据只有<code>true（真）</code>和<code>false（假）</code>两个值。</p><ul><li>注意：</li></ul><ol><li>布尔类型变量的默认值为false。</li><li>Go 语言中不允许将整型强制转换为布尔型.</li><li>布尔型无法参与数值运算，也无法与其他类型进行转换。</li></ol><h3 id="字符串"><a class="markdownIt-Anchor" href="#字符串"></a> 字符串</h3><p>Go语言中的字符串以原生数据类型出现，使用字符串就像使用其他原生数据类型（int、bool、float32、float64 等）一样。 Go 语言里的字符串的内部实现使用<code>UTF-8</code>编码。 字符串的值为<code>双引号(&quot;)</code>中的内容，可以在Go语言的源码中直接添加非ASCII码字符，例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">s1 := &quot;hello&quot;</span><br><span class="line">s2 := &quot;你好&quot;</span><br></pre></td></tr></table></figure><h4 id="字符串转义符"><a class="markdownIt-Anchor" href="#字符串转义符"></a> 字符串转义符</h4><p>Go 语言的字符串常见转义符包含回车、换行、单双引号、制表符等，如下表所示。</p><table>    <tr>        <th>转义符</th>        <th>含义</th>    </tr>    <tr>        <th>\r</th>        <th>回车符（返回行首）</th>    </tr>    <tr>        <th>\n</th>        <th>换行符（直接跳到下一行的同列位置）</th>    </tr>    <tr>        <th>\t</th>        <th>制表符</th>    </tr>    <tr>        <th>\'</th>        <th>单引号</th>    </tr>    <tr>        <th>\"</th>        <th>双引号</th>    </tr>    <tr>        <th>\\</th>        <th>反斜杠</th>    </tr></table><p>举个例子，我们要打印一个Windows平台下的一个文件路径：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">package main</span><br><span class="line">import (</span><br><span class="line">    &quot;fmt&quot;</span><br><span class="line">)</span><br><span class="line">func main() &#123;</span><br><span class="line">    fmt.Println(&quot;str := \&quot;c:\\Code\\lesson1\\go.exe\&quot;&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="多行字符串"><a class="markdownIt-Anchor" href="#多行字符串"></a> 多行字符串</h4><p>Go语言中要定义一个多行字符串时，就必须使用<code>反引号</code>字符：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">s1 := `第一行</span><br><span class="line">第二行</span><br><span class="line">第三行</span><br><span class="line">`</span><br><span class="line">fmt.Println(s1)</span><br></pre></td></tr></table></figure><p>反引号间换行将被作为字符串中的换行，但是所有的转义字符均无效，文本将会原样输出。</p><h4 id="字符串的常用操作"><a class="markdownIt-Anchor" href="#字符串的常用操作"></a> 字符串的常用操作</h4><table>    <tr>        <th>方法</th>        <th>介绍</th>    </tr>    <tr>        <th>len(str)</th>        <th>求长度</th>    </tr>    <tr>        <th>+或fmt.Sprintf</th>        <th>拼接字符串</th>    </tr>    <tr>        <th>strings.Split</th>        <th>分割</th>    </tr>    <tr>        <th>strings.contains</th>        <th>判断是否包含</th>    </tr>    <tr>        <th>strings.HasPrefix,strings.HasSuffix</th>        <th>前缀/后缀判断</th>    </tr>    <tr>        <th>strings.Index(),strings.LastIndex()</th>        <th>子串出现的位置</th>    </tr>    <tr>        <th>strings.Join(a[]string, sep string)</th>        <th>join操作</th>    </tr></table><h3 id="byte和rune类型"><a class="markdownIt-Anchor" href="#byte和rune类型"></a> byte和rune类型</h3><p>组成每个字符串的元素叫做“字符”，可以通过遍历或者单个获取字符串元素获得字符。 字符用单引号（’）包裹起来，如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">var a = &#x27;中&#x27;</span><br><span class="line">var b = &#x27;x&#x27;</span><br></pre></td></tr></table></figure><p>Go 语言的字符有以下两种：</p><ol><li>uint8类型，或者叫 byte 型，代表一个ASCII码字符。</li><li>rune类型，代表一个 UTF-8字符。</li></ol><p>当需要处理中文、日文或者其他复合字符时，则需要用到<code>rune</code>类型。<code>rune</code>类型实际是一个<code>int32</code>。</p><p>Go 使用了特殊的 rune 类型来处理 Unicode，让基于 Unicode 的文本处理更为方便，也可以使用 byte 型进行默认字符串处理，性能和扩展性都有照顾。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">// 遍历字符串</span><br><span class="line">func traversalString() &#123;</span><br><span class="line">s := &quot;hello沙河&quot;</span><br><span class="line">for i := 0; i &lt; len(s); i++ &#123; //byte</span><br><span class="line">fmt.Printf(&quot;%v(%c) &quot;, s[i], s[i])</span><br><span class="line">&#125;</span><br><span class="line">fmt.Println()</span><br><span class="line">for _, r := range s &#123; //rune</span><br><span class="line">fmt.Printf(&quot;%v(%c) &quot;, r, r)</span><br><span class="line">&#125;</span><br><span class="line">fmt.Println()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">104(h) 101(e) 108(l) 108(l) 111(o) 230(æ) 178(²) 153() 230(æ) 178(²) 179(³) </span><br><span class="line">104(h) 101(e) 108(l) 108(l) 111(o) 27801(沙) 27827(河) </span><br></pre></td></tr></table></figure><p>因为UTF8编码下一个中文汉字由3~4个字节组成，所以我们不能简单的按照字节去遍历一个包含中文的字符串，否则就会出现上面输出中第一行的结果。</p><p>字符串底层是一个byte数组，所以可以和<code>[]byte</code>类型相互转换。字符串是不能修改的 字符串是由byte字节组成，所以字符串的长度是byte字节的长度。 rune类型用来表示utf8字符，一个rune字符由一个或多个byte组成。</p><h4 id="修改字符串"><a class="markdownIt-Anchor" href="#修改字符串"></a> 修改字符串</h4><p>要修改字符串，需要先将其转换成<code>[]rune</code>或<code>[]byte</code>，完成后再转换为<code>string</code>。无论哪种转换，都会重新分配内存，并复制字节数组。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">func changeString() &#123;</span><br><span class="line">s1 := &quot;big&quot;</span><br><span class="line">// 强制类型转换</span><br><span class="line">byteS1 := []byte(s1)</span><br><span class="line">byteS1[0] = &#x27;p&#x27;</span><br><span class="line">fmt.Println(string(byteS1))</span><br><span class="line"></span><br><span class="line">s2 := &quot;白萝卜&quot;</span><br><span class="line">runeS2 := []rune(s2)</span><br><span class="line">runeS2[0] = &#x27;红&#x27;</span><br><span class="line">fmt.Println(string(runeS2))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="类型转换"><a class="markdownIt-Anchor" href="#类型转换"></a> 类型转换</h3><p>Go语言中只有强制类型转换，没有隐式类型转换。该语法只能在两个类型之间支持相互转换的时候使用。</p><p>强制类型转换的基本语法如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T(表达式)</span><br></pre></td></tr></table></figure><p>其中，T表示要转换的类型。表达式包括变量、复杂算子和函数返回值等.</p><p>比如计算直角三角形的斜边长时使用math包的Sqrt()函数，该函数接收的是float64类型的参数，而变量a和b都是int类型的，这个时候就需要将a和b强制类型转换为float64类型。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">func sqrtDemo() &#123;</span><br><span class="line">var a, b = 3, 4</span><br><span class="line">var c int</span><br><span class="line">// math.Sqrt()接收的参数是float64类型，需要强制转换</span><br><span class="line">c = int(math.Sqrt(float64(a*a + b*b)))</span><br><span class="line">fmt.Println(c)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="自主练习题"><a class="markdownIt-Anchor" href="#自主练习题"></a> 自主练习题</h2><ol><li>编写代码分别定义一个整型、浮点型、布尔型、字符串型变量，使用fmt.Printf()搭配%T分别打印出上述变量的值和类型。</li><li>编写代码统计出字符串&quot;hello沙河小王子&quot;中汉字的数量。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;来源是b站&lt;a href=&quot;https://www.bilibili.com/video/BV1qb411M785?spm_id_from=333.788.videopod.sections&amp;amp;vd_source=d327742890f6b7694d0a2026350</summary>
      
    
    
    
    
    <category term="Golang" scheme="http://example.com/tags/Golang/"/>
    
  </entry>
  
  <entry>
    <title>记录学习前端开发的第一篇笔记</title>
    <link href="http://example.com/2024/11/28/Learn-html/"/>
    <id>http://example.com/2024/11/28/Learn-html/</id>
    <published>2024-11-27T16:00:00.000Z</published>
    <updated>2025-01-02T15:24:21.762Z</updated>
    
    <content type="html"><![CDATA[<p>来源是<a href="https://www.bilibili.com/video/BV1kM4y127Li/?p=3&amp;spm_id_from=333.880.my_history.page.click&amp;vd_source=d327742890f6b7694d0a202635037cec">b站</a>前端Web开发HTML5+CSS3+移动web视频教程</p><h2 id="常用的标签"><a class="markdownIt-Anchor" href="#常用的标签"></a> 常用的标签</h2><h3 id="标题标签"><a class="markdownIt-Anchor" href="#标题标签"></a> 标题标签</h3><p>标签名：<code>h1 ~ h6（双标签）</code><br />显示特点：</p><ul><li>文字加粗</li><li>字号逐渐减小</li><li>独占一行（换行）</li></ul><p>h1标签在一个网页中只能使用一次，用来放标题或网页的logo<br />h2 ~ h6没有使用次数限制</p><h3 id="段落标签"><a class="markdownIt-Anchor" href="#段落标签"></a> 段落标签</h3><p>标签名：<code>p（双标签）</code><br />显示特点：</p><ul><li>独占一行</li><li>段落之间存在间隙</li></ul><h3 id="换行"><a class="markdownIt-Anchor" href="#换行"></a> 换行</h3><p>标签名：<code>&lt;br&gt;（单标签）</code></p><h3 id="水平线"><a class="markdownIt-Anchor" href="#水平线"></a> 水平线</h3><p>标签名：<code>&lt;hr&gt;（单标签）</code></p><h3 id="文本格式化标签"><a class="markdownIt-Anchor" href="#文本格式化标签"></a> 文本格式化标签</h3><p>作用：为文本添加格式，以突出重点。常见的文本格式：<b>加粗</b>、<i>倾斜</i>、<u>下划线</u>、<s>删除线</s>等。</p><p>标签名（常用）  效果    （少用）<br />strong  <strong>加粗</strong>   b<br />em  <em>倾斜</em>   i<br />ins <ins>下划线</ins>   u<br />del <del>删除线</del>   s</p><p><code>strong、em、ins、del</code>标签自带强调含义（语义）</p><h3 id="图像标签"><a class="markdownIt-Anchor" href="#图像标签"></a> 图像标签</h3><p>作用：在网页中插入图片</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;img src=&quot;图片的URL&quot;&gt;</span><br></pre></td></tr></table></figure><p><code>src</code>用于指定图像的位置和名称，是<code>&lt;img&gt;</code>的必须属性</p><p>属性    作用    说明<br />alt 替换文本    图片无法显示的时候显示的文字<br />title   提示文本    鼠标悬停在图片上面的时候显示的文字<br />width   图片的宽度  值为数字，没有单位<br />height  图片的高度  值为数字，没有单位</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;来源是&lt;a href=&quot;https://www.bilibili.com/video/BV1kM4y127Li/?p=3&amp;amp;spm_id_from=333.880.my_history.page.click&amp;amp;vd_source=d327742890f6b769</summary>
      
    
    
    
    
    <category term="HTML" scheme="http://example.com/tags/HTML/"/>
    
  </entry>
  
</feed>
